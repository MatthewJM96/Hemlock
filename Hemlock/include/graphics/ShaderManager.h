#pragma once

#ifndef HEMLOCK_USING_PCH
#include "types.h"
#endif

namespace hemlock {
    namespace graphics {
        class ShaderManager {
        public:
            ShaderManager() {};
            ~ShaderManager() {};

        private:

        };
    }
}
namespace hg = hemlock::graphics;
