#pragma once

namespace hemlock {
    namespace ui {
        enum class MouseButton {
            LEFT,
            RIGHT,
            MIDDLE,
            UNKOWN
        };

        class InputManager {

        };
    }
}
namespace hui = hemlock::ui;
